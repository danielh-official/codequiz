#!/usr/bin/env node
// codequiz — Claude Code entry point. The hook itself is core/hook.js, shared
// verbatim with Codex; this file exists because the plugin must be
// self-contained (org sync packages adapters/claude-code/ alone) and because
// hooks.json needs a stable path under ${CLAUDE_PLUGIN_ROOT}.
// Vendored copies are regenerated by `npm run sync`.

require('../core/hook').run();
