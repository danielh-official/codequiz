// GENERATED by scripts/sync.js from core/hook.js — edit that file, then run `npm run sync`.
// codequiz — the UserPromptSubmit hook.
//
// Not host-free logic: this is one wire contract that Claude Code and Codex
// both implement, byte for byte. Input carries `prompt` and `cwd`; output takes
// `hookSpecificOutput.additionalContext` and `systemMessage`. Codex pins it in
// codex-rs/hooks/schema/generated/user-prompt-submit.command.{input,output}.schema.json.
//
// Claude Code runs a vendored copy of this file (see scripts/sync.js); Codex
// runs it directly out of the clone.

const { decide } = require('./decide');
const { readState, writeState } = require('./state');

function writeHookOutput(additionalContext, systemMessage) {
  const out = {};
  if (systemMessage) out.systemMessage = systemMessage;
  if (additionalContext) {
    out.hookSpecificOutput = {
      hookEventName: 'UserPromptSubmit',
      additionalContext,
    };
  }
  process.stdout.write(JSON.stringify(out));
}

function main(raw) {
  const data = JSON.parse(String(raw).replace(/^﻿/, ''));
  const result = decide(data.prompt, readState(), Date.now(), data.cwd);

  if (result.state) writeState(result.state);
  if (result.action === 'command') writeHookOutput(`codequiz: ${result.message}. Acknowledge in one short line.`, result.message);
  else if (result.action === 'ask') writeHookOutput(result.context);
}

// Both hosts block the prompt on this process. Every failure path exits quietly
// so a broken quizzer can never cost the user their turn.
function run() {
  let input = '';
  process.stdin.on('data', (c) => { input += c; });
  process.stdin.on('end', () => {
    try {
      main(input);
    } catch (e) {
      // Silent by design.
    }
  });
}

if (require.main === module) run();

module.exports = { main, run, writeHookOutput };
